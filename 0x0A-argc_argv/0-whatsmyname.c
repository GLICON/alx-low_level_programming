i#include <stdio.h>
#include <stdlib.h>

/**
 * main - to print the program name
 * @argc: argument count
 * @argv: argument array
 *
 * Return: Always 0 (Success)
 */

int main (int argc, char* argv[])
{
printf("%s\n", *argv);

return (0);
}
