#include "main.h"

/**
 * jack_bauer - Entry
 * Return: 0 Always (Success)
 */
void jack_bauer(void)
{
int x;
int y;
int z;
int w;
        for (w = 0; w <= 2; w++)
        {
		for (x = 0; x <= 3; x++)

                i = -i;
        }
        n = (i % 10);
        if (n < 0)
        {
                n = -n;
        }
        _putchar(n + '0');
        return (n);
}
