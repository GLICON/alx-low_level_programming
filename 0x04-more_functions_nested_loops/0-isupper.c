#include <main.h>

/**
 * _isupper - A function that checks for uppercase character
 * @c: charatere used
 * Return: 1 if uppercase, otherwise 0
 */

int _isupper(int c)
{
	for (c = 'A'; c <= 'Z'; c++)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
