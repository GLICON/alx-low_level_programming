#include "main.h"
#include <stdio.h>

/**
 * mul - A function that multiplies two integers
 * @a: first integer
 * @b: second integer
 * Return: Always 0 (Success)
 */

int mul(int a, int b);
{
	printf("%d", a * b);
	return (a * b);
}

