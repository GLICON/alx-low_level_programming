#include <stdio.h>

/**
 * main - A program that prints the alphabet in lowercase
 *
 * Return: Always return 0 (Success)
 */

int main(void)
{
	char w;

	for (c = 'a'; c = 'z'; c++)
	{
	putchar(c)
	}
	putchar ("\n");
	return (0);
}
